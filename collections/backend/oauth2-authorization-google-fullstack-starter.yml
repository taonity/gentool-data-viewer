info:
  name: /oauth2/authorization/google-gentool-data-viewer
  type: http
  seq: 4

http:
  method: GET
  url: "{{backendBaseUrl}}/oauth2/authorization/google-gentool-data-viewer"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
